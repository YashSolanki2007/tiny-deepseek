                                                         Learning to Skip for Language Modeling

                                            Output                              most computationally intensive component of a Transformer
                                                                                model, but can be applied to a batch of tokens in a point-
                                             FFN
                                                                                wise manner. Therefore, each input token of a batch can
                                                                                activate the FFFN layer independently with the probability
                                                 LN
                                                                                P . Because FFFN consumes most of the computation in a
                                                                                Transformer layer, we can thus have big savings in FLOPs
                     Self attention
                                             Attn
                                                                                when the activation probability P is small.
                                                                                The self-attention layer FAttn consumes much less computa-
                        key, value           query                              tion relative to the FFFN layer. However, FAttn cannot apply
                                                                                to a batch of tokens in the pointwise way because tokens
                                      activate
                                                                                need to attend to each other to compute their own attention
                           LN               router
                                                      skip                      output. If most tokens in a batch are skipped when P is
                                                                                small, the left non-skipped tokens will lose most of the con-
                          Token                                                 text of the respective sequences they belong to. We also
                                                                                empirically observe lower predictive quality when this sim-
    Figure 3. Overview of our SkipLayer for Transformer-based mod-              ple skipping mechanism is applied. Therefore, we propose
    els, example of a single layer. LN is the layer normalization layer,        the following partial skipping mechanism. As shown in
    query, key and value refers to the computation of query, key and            Figure 3, when the input tokens are skipped, their key and
    value projections in the self-attention layer. Attn is the attention        value projections are still preserved (Line 2 in Algorithm 1)
    computation. Residual connections in the self attention layer and           since they are part of the context and are needed for the rest
    FFN layer are ignored for simplicity.                                       non-skipped tokens to further attend to. However, because
                                                                                the skipped tokens do not require attention calculations by
    Algorithm 1 Forward pass of SkipLayer                                       themselves, we can still omit their query projections. It
   Data: A batch of tokens X ∈ RB×T ×d , target probability P .                 is worth to mention that Line 3-11 in Algorithm 1 can be
1  Get the mask M by Equation 3.                                                computed in parallel using our efficient implementation in
 2 Get the key and value projection K ← Fkey (X), V ← Fval (X).
 3 for b ≤ B, t ≤ T do
                                                                                Section 2.2 to increase training speed.
 4      if M [b, t] = 1 then                                                    Algorithm 2 shows the greedy decoding logic of one
 5           Get the query projection q ← Fquery (X[b, t])
                                                                                SkipLayer-based Transformer layer. The router makes the
 6           x′ ← FAttn (FLN (X[b, t])|K, q, V ) · M [b, t] + X[b, t]
 7           XSL [b, t] ← FFFN (FLN (x′ )) + x′                                 skipping decision by picking the most likely outcome. The
 8      else                                                                    key and value projections will be computed and saved in
 9           XSL [b, t] ← X[b, t]) · (1 − M [b, t])                             the decoding cache (Line 2). Only when the router activates
10      end                                                                     the current layer, the query projection will be computed for
11 end                                                                          current token, and then the decoding cache K and V which
             P                             2
12 ℓaux ← (     b,t M [b, t]/(B · T ) − P )                                     contain the key and value projections of previous decoding
13 return XSL , ℓaux                                                            steps will be used to compute the self attention. Otherwise,
                                                                                there will be no further computations from this layer.
    2.3. SkipLayer for Transformer-based Models                                 Algorithm 2 SkipLayer per decoding step
    In this section, we focus in particular on applying SkipLayer             Data: The current state x ∈ Rd , key and value cache K, V
    to Transformer-based decoder-only language models in the                1 m = G(x|WG ) = argmax x⊤ WG
                                                                            2 K ← Fkey (x), V ← Fval (x)
    setup of in-context learning. A Transformer layer mainly                3 if m = 1 then
    includes the self-attention, layer normalization, and FFN as            4      Get the query projection q ← Fquery (x)
    the sub-layers, and can be represented as                               5      x′ ← FAttn (FLN (x)|K, q, V ) + x
                       ′                                                    6      xSL ← FFFN (FLN (x′ )) + x′
                    X l = FAttn FLN (X l ) + X l ,
                                            
                                                                            7 else
                                           ′        ′                              xSL ← x
                                                                          8
                  X l+1 = FFFN FLN (X l ) + X l .             (5)           9 end
                                                                           10 return: xSL
    SkipLayer can be applied to a single Transformer layer
    shown in Figure 3. We propose to wrap the entire Trans-
    former layer into a SkipLayer to preserve the atomicity of                  3. Experiment Setup
    the self-attention (FAttn ) → FFN (FFFN ) structure. How-
    ever, the FAttn layer and the FFFN layer have slightly differ-              We focus on training decoder-only language models. This
    ent skipping implementations. The FFFN layer is often the                   section elaborates our training setup, hyperparameters, base-
